export const AUTH_COOKIE_NAME = "lyra_dashboard_session";

/** Session storage key for orchestration enqueue secret (same as Bearer for gated /api routes). */
export const LYRA_ENQUEUE_SECRET_STORAGE_KEY = "lyra_enqueue_secret";
